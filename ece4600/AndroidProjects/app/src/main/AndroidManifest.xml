<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"> xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rob.wifionoff">
        <uses-permission> android:="android.permission.INTERNET"></uses-permission>
        <uses-permission> android:="android.permission.ACCESS_NETWORK.ACCESS></uses-permission>


        <-! wip ->
        <uses-sdk
            android:minSdkVersion = "7"
            android:targetSdkVersions="17" ></>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="tcpHandler">
        <activity android:name=".mainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>
</manifest>
